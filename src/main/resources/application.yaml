server:
  port: 8080

spring:
  application:
    name: "Gateway Service"
  datasource:
    url: jdbc:mysql://localhost:3306/gatewaydb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  liquibase:
    enabled: false
    change-log: classpath:/db.changelog/changelog-master.xml

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /api/gateway/swagger-ui.html